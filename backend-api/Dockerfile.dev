# 后端API服务 开发环境 Dockerfile
FROM node:18-alpine

# 安装开发工具
RUN apk add --no-cache python3 make g++

# 设置工作目录
WORKDIR /app

# 复制 package.json 文件
COPY package*.json ./

# 安装依赖（包括开发依赖）
RUN npm install

# 安装 nodemon 全局
RUN npm install -g nodemon

# 创建必要的目录
RUN mkdir -p logs public/uploads/articles public/uploads

# 暴露端口
EXPOSE 3001

# 开发模式启动（使用 nodemon）
CMD ["nodemon", "src/server.js"]